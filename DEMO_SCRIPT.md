# StepUp — AI 활용 경진대회 시연 영상 대본

> **버전**: 2026-08-02 기준 (실제 배포 화면 + DEVLOG.md 기준으로 전면 재작성 — 기존 6월판 대체)
> **총 소요 시간**: 약 6분 (페이지별 타임스탬프 참고해 자유롭게 가감 가능)
> **시연 캐릭터**: 공예 작가를 위한 온라인 판매 플랫폼을 준비 중인 예비창업자
> **핵심 메시지**: "AI가 창업 코칭 전 과정에 실제로 개입한다 — 초안만 써주는 게 아니라, 채점하고, 근거를 대며 피드백하고, 고친 걸 다시 비교하고, 실제 정부 데이터를 매칭한다."

---

## 준비사항

- [ ] 테스트 계정 로그인 상태 (관심분야: 공예, 지역: 서울, 아이템 키워드 입력해둔 상태)
- [ ] STEP 1~6은 미리 채워두고, STEP 4 정도를 라이브 시연용으로 비워두기
- [ ] 다크모드/라이트모드 전환 한 번 보여줄 것 (테마 시스템 어필)
- [ ] 브라우저 시크릿 창

---

## 0. 랜딩 페이지 — `/` (30초)

**보여줄 것**: 히어로 문구 → "실시간 반영 공고 기관" 워드마크 마퀴 → 7단계 로드맵 타임라인 → 스마트 매칭 통계

**멘트**
> "StepUp은 예술·콘텐츠 창업자가 아이디어를 7단계 로드맵으로 완성하고, 그 과정에서 실제 정부 지원사업까지 자동으로 매칭받는 서비스입니다. 상단에 보이는 중기부·문체부·K-Startup 같은 기관들은 저희가 실제로 공개 데이터를 가져오는 곳이에요 — 이건 뒤에서 직접 보여드리겠습니다."

**기술 포인트**: Next.js 16(App Router) 정적 export로 GitHub Pages에 무료 배포. 5색 팔레트 × 라이트/다크 테마를 CSS 변수 토큰으로 관리해서, 화면 우측 상단 팔레트 아이콘으로 실시간 전환 가능(여기서 한 번 눌러서 보여주기 좋음).

---

## 1. 회원가입 — `/signup` (건너뛰거나 15초)

**보여줄 것**: 관심분야(고정 6종 카테고리)·지역(자유 텍스트)·창업 아이템 키워드 입력 폼

**멘트**
> "가입할 때 받는 관심분야·지역·아이템 키워드, 이 세 가지가 이후 모든 개인화 추천의 기준이 됩니다."

---

## 2. 대시보드 — `/dashboard` (45초)

**보여줄 것**
1. 상단 "오늘의 미션" 카드 — 지금 상황에 맞는 CTA 하나만 (로그인 여부·진행률에 따라 자동 분기)
2. "지금 신청 가능한 지원사업" 카드 — **"맞춤" 배지**가 붙은 항목
3. 하단 "창업 정보 허브" 3열(Startup News / 정부지원사업 소식 / Recommended Articles)

**멘트**
> "대시보드는 '지금 뭘 해야 하는지' 하나만 크게 보여줍니다. 오른쪽 지원사업 카드를 보시면 제 관심분야(공예)·지역(서울)과 일치하는 공고에 '맞춤' 배지가 붙어 우선 노출돼요 — 프로필 정보가 없는 유저는 기존처럼 진행 단계 기준으로 자동 대체되니 결과가 0건이 되는 일은 없습니다. 아래 정보 허브의 '정부지원사업 소식'과 'Startup News'는 매일 새벽 K-Startup·기업마당·KOCCA·플래텀·벤처스퀘어에서 실제로 수집한 데이터예요."

**기술 포인트**: FastAPI + PostgreSQL(Render) 백엔드. 매칭 로직은 관심분야는 정확 일치, 지역은 자유 텍스트라 느슨한 부분일치로 계산. 정보 허브는 GitHub Actions 크론이 매일 `/hub/collect`를 호출해 공식 API(K-Startup)·공개 목록 크롤링(기업마당, KOCCA)·RSS(플래텀, 벤처스퀘어, 바이라인네트워크)로 실데이터를 수집·태깅해서 DB에 저장하는 구조.

---

## 3. 로드맵 STEP 페이지 — `/roadmap/4` (2분, 핵심 구간)

**보여줄 것 — 순서대로**
1. 좌측 사이드바: 7단계 진행률 + 이 단계 추천 지원사업
2. **"AI 초안 생성 시작"** 클릭 → Solar AI가 프레임워크 표(수익원/가격전략/비용구조/단위경제)를 채움
3. **"완성도 채점"** — 점수(0~100) + 등급(A~D) + "보완 필요" 항목 + 구체적 개선 팁
4. **"코치 요다의 피드백"** — 방법론 근거(예: TPCS, 유닛 이코노믹스)를 들어 설명
5. 내용을 일부러 고친 뒤 **"피드백 다시 받기"** → 이전/이후 비교(+N점 향상, 개선된 점, 남은 과제)

**멘트**
> "AI 초안 생성을 누르면 Solar AI가 제가 입력한 아이템 키워드를 바탕으로 이 단계의 모든 항목을 채웁니다. 그런데 여기서 끝이 아니라, 바로 아래에서 완성도를 0~100점, A~D 등급으로 자동 채점하고 '수익 모델이 불명확하다'처럼 구체적으로 뭐가 부족한지 짚어줍니다. 오른쪽 코치 요다는 그냥 칭찬이 아니라 실제 창업 방법론(TPCS, 유닛 이코노믹스 등)을 근거로 피드백을 주고요. 내용을 고친 뒤 피드백을 다시 받으면, AI가 이전 버전과 비교해서 정확히 뭐가 좋아졌고 뭐가 아직 남았는지까지 알려줍니다 — 한 번 생성하고 끝이 아니라 계속 대화하면서 완성도를 끌어올리는 구조예요."

**기술 포인트**: Solar API(`solar-pro`, Upstage) 호출을 단계별로 4단계 파이프라인(초안 생성 → 채점 → 피드백 → 비교)으로 분리. 각 단계 프롬프트에 방법론 원문(린 스타트업, TAM·SAM·SOM 등)을 컨텍스트로 주입해 "AI가 그럴듯하게 지어내는" 게 아니라 근거 기반으로 답하도록 설계. 편집용 textarea는 포커스 링·ARIA까지 접근성 기준(WCAG AA)에 맞춰 다듬었음.

---

## 4. 지원사업 매칭 — `/programs` (1분)

**보여줄 것**
1. 검색폼: 창업 아이템 / **로드맵 STEP(1~7)** / 분야 / 지역
2. "맞춤 지원사업 추천받기" 클릭 → 즉시 결과
3. 조건에 맞는 카드만 **보라색 테두리 + "맞춤" 배지**, 나머지도 목록에서 사라지지 않고 그대로 노출

**멘트**
> "지원사업 페이지는 STEP·분야·지역 세 조건으로 실제 검증된 지원사업 목록을 그 자리에서 바로 필터링합니다. 조건에 맞는 공고만 테두리로 강조되고, 나머지도 사라지지 않아요 — 조건이 안 맞아도 지금 신청 가능한 다른 기회를 놓치지 않게 하려는 설계입니다. '신청 조건 확인하기'를 누르면 실제 K-Startup·기업마당·KOCCA·예술경영지원센터 공고 원문으로 바로 연결돼요."

**기술 포인트**: 원래 프로젝트 초창기의 LangChain+FAISS RAG로 매칭했었는데, 클라우드 배포 환경에서 인덱스가 매번 사라지는 문제가 있어 지금은 실제 검증된 지원사업 데이터를 클라이언트에서 즉시 필터링·정렬하는 방식으로 교체 — 네트워크 지연 없이 즉시 결과가 나온다.

---

## 5. 사업계획서 — `/business-plan` (45초)

**보여줄 것**
1. 7단계 내용이 문제 발견 → ... → 피치덱 순서로 자동 통합된 문서
2. 우측 "코치 요다의 피드백" — 전체 계획서 종합 평가
3. "편집" → 직접 수정 → "저장 완료" → "피드백 다시 받기"
4. "사업계획서 복사하기"

**멘트**
> "7단계를 모두 완료하면 Solar AI가 지금까지 쓴 내용을 하나의 사업계획서로 자동 통합합니다. 편집 버튼으로 직접 다듬을 수 있고, 고친 뒤 피드백을 다시 받으면 코치 요다가 전체 맥락에서 다시 검토해줘요. 완성되면 복사 버튼 하나로 실제 지원사업 신청서에 바로 붙여넣을 수 있습니다."

---

## 6. (선택) 이용 가이드 · 요금제 — `/guide`, `/pricing` (짧게, 30초 또는 생략)

**멘트**
> "저희 서비스는 실제 화면을 그대로 캡처한 이용 가이드 페이지도 별도로 제공하고 있고, 지금은 베타 기간이라 유료 요금제(Pro·Business) 기능까지 전부 무료로 열려있습니다."

*(시간이 빠듯하면 이 섹션은 생략 가능)*

---

## 7. 클로징 — 기술 스택 요약 (30초)

**멘트**
> "정리하면, StepUp은 Next.js 프론트엔드와 FastAPI·PostgreSQL 백엔드가 실제로 연결된 풀스택 서비스이고, 그 위에서 Solar AI가 초안 생성·완성도 채점·근거 기반 피드백·이전 대비 비교·사업계획서 통합까지 창업 코칭의 전 과정에 개입합니다. 그리고 여기서 그치지 않고, K-Startup 공식 API와 기업마당·KOCCA 공개 데이터, 플래텀·벤처스퀘어 RSS까지 매일 자동으로 수집해 실제 사용자 프로필과 매칭하는 데이터 파이프라인까지 직접 만들었습니다."

---

## 부록 A — 페이지별 "어떻게 쓰는지" 한 줄 요약

| 페이지 | 핵심 사용법 |
|---|---|
| `/` (랜딩) | 서비스 소개, "무료로 시작하기"로 가입 유도 |
| `/signup` | 관심분야·지역·아이템 키워드 입력 → 이후 모든 개인화의 기준 |
| `/dashboard` | 로그인 후 첫 화면. "오늘의 미션" 버튼 하나만 누르면 됨 |
| `/roadmap/{1~7}` | AI 초안 생성 → 직접 수정 → 채점·피드백 확인 → 저장 후 다음 단계 |
| `/programs` | STEP·분야·지역 입력 → 맞춤 지원사업 확인 → 신청 페이지로 이동 |
| `/business-plan` | 7단계 완료 후 자동 생성된 문서 편집·피드백·복사 |
| `/guide` | 실제 화면 캡처 기반 사용법 안내 (신규 유저용) |
| `/pricing`, `/beta` | 요금제 확인, 베타 기간 무료 이용 안내 |

## 부록 B — 페이지별 "무슨 기술을 썼는지" 한 줄 요약

| 영역 | 기술 |
|---|---|
| 프론트엔드 | Next.js 16(App Router, 정적 export) · TypeScript · Tailwind CSS v4(디자인 토큰) |
| 백엔드 | FastAPI(Python) · PostgreSQL · SQLAlchemy · Render 배포 |
| AI | Solar API(`solar-pro`, Upstage) — 초안 생성/채점/피드백/비교/사업계획서 통합, 방법론 원문을 프롬프트에 직접 주입 |
| 실데이터 수집 | K-Startup 공식 오픈API + 기업마당·KOCCA 공개 페이지 크롤링 + RSS(플래텀·벤처스퀘어·바이라인네트워크), GitHub Actions 크론으로 매일 자동 수집 |
| 개인화 | 관심분야(정확 매칭)·지역(느슨한 매칭)·로드맵 STEP 3중 기준, 프로필 없으면 자동 폴백 |
| 보안 | JWT 인증·재발급, 입력값 검증, slowapi 레이트리밋 |
| 접근성 | WCAG AA 색 대비, 포커스 링/포커스 이동·복귀, ARIA 라벨 |

## 부록 C — 예상 Q&A

**Q. 지원사업 데이터는 실시간인가요?**
A. 네. GitHub Actions 크론이 매일 새벽 K-Startup 공식 API, 기업마당·KOCCA 공개 페이지, 플래텀·벤처스퀘어 RSS를 자동으로 수집해 DB에 반영합니다. 수집 전이거나 일시적으로 비어있을 땐 라이브로 검증해둔 실제 공고로 안전하게 대체됩니다.

**Q. AI 초안이 부정확하면요?**
A. 모든 항목이 편집 가능한 텍스트라 직접 고칠 수 있고, 고친 뒤 "피드백 다시 받기"를 누르면 수정 내용 기준으로 다시 채점·비교해줍니다.

**Q. 왜 Solar API를 썼나요?**
A. 한국어 창업 도메인 코칭에 최적화된 한국어 LLM이 필요했고, 방법론 근거를 프롬프트에 직접 주입하는 구조상 응답 품질과 비용 효율이 중요했습니다.
