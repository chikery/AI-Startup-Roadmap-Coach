# StepUp — UI 재설계 프롬프트 (Claude Code용)

## 역할

너는 시니어 프로덕트 디자이너이자 프론트엔드 엔지니어다. 기존 UI를 단순히 예쁘게 리디자인하지 마라. 현재 화면을 **기능 중심(Function-driven UI)에서 사용자 행동 중심(Task-driven UI)으로 재설계**하는 것이 목표다.

최종 목표는 이 서비스가 단순한 "Roadmap Generator"가 아니라, 창업자가 매일 접속해 "오늘 무엇을 해야 하는가"를 확인하는 **AI Startup Operating System**처럼 느껴지게 만드는 것이다.

---

## 서비스 컨텍스트 (StepUp)

- 대상: 아이디어만 가진 청년·문화예술 예비창업자
- 핵심 가치: 7단계 로드맵을 따라가며 AI 코칭으로 사업계획서를 완성하고, 현재 단계에 맞는 지원사업을 추천받는다
- 7단계: ①문제 발견 ②시장·고객 리서치 ③솔루션 & MVP 설계 ④비즈니스 모델 ⑤실행 로드맵 ⑥재무 계획 ⑦피치덱 완성
- 각 단계 패턴: 단계 설명 → AI 초안 생성(Solar LLM) → 사용자 수정 → 완성도 채점 → 코치 요다 피드백 → 저장 후 다음 단계
- 부가: 저장 시 완성도·자격 건수 갱신, 현재 단계에 맞는 지원사업 추천
- 기술 스택: Next.js(GitHub Pages), FastAPI(Render), PostgreSQL, JWT, Solar API

## 현재 문제점

현재 UI는 정보를 나열하는 기능 중심 구조다. 대시보드에 현재 단계·완성도·로드맵·추천 사업이 모두 평등하게 놓여 있어, 사용자가 "지금 당장 무엇을 눌러야 하는지" 스스로 판단해야 한다. 로드맵이 하나의 독립 메뉴처럼 취급되고, 화면마다 다음 행동을 유도하는 단일 CTA가 약하다.

---

## 작업 지시

### 1단계 — 사용자 행동(Task) 정의

코드를 짜기 전에, 이 서비스에서 사용자가 실제로 수행하는 행동을 먼저 목록화하라. 최소 아래를 포함하되, 서비스 맥락에 맞게 추가·수정하라.

- 아이디어 작성 / 아이템 정의
- 오늘 해야 할 일 확인
- 현재 단계 이어서 작업하기
- AI 초안 생성 및 수정
- 코치 요다에게 피드백 받기
- 완성도·진행률 확인
- 지금 신청 가능한 지원사업 확인
- 사업계획서/피치덱 내보내기
- 창업 자료·방법론 학습

각 행동에 대해 "언제, 어떤 맥락에서, 무엇을 기대하며 이 행동을 하는가"를 한 줄로 정의하라.

### 2단계 — 행동별 화면 설계 (User Journey 기반)

기능을 배치하지 말고, 사용자의 행동 흐름에 맞게 화면을 재구성하라.

- **Home = Dashboard**: "오늘 무엇을 해야 하는가"만 보여준다. 사용자의 현재 상태를 보여주는 운영체제(OS)처럼 구성하라. 접속 즉시 "다음에 할 단 하나의 행동"이 화면에서 가장 크고 명확해야 한다.
- **Roadmap은 독립 메뉴가 아니라 Dashboard 내부 기능 중 하나**로 편입하라. 대시보드에서 현재 단계 카드를 누르면 자연스럽게 작업 화면으로 진입하는 흐름으로 만든다.
- 모든 화면은 사용자의 **다음 행동을 유도하는 CTA를 반드시 하나 이상** 포함한다. 화면마다 "이 화면에서 사용자가 해야 할 단 하나의 핵심 행동"을 정하고, 그것을 시각적으로 가장 강조하라. 나머지 정보는 그 행동을 돕는 보조로 배치한다.

### 3단계 — 서비스 전용 Component 설계

범용 UI 컴포넌트가 아니라, 이 서비스의 행동에 맞는 도메인 전용 컴포넌트로 재설계하라. 아래는 예시이며, 서비스에 맞게 이름과 역할을 정의하라.

- `TodayMissionCard` — 오늘 해야 할 단 하나의 핵심 작업
- `RoadmapProgressCard` — 7단계 진행 상태와 다음 단계
- `MilestoneCard` — 방금 완료한 단계의 성취 표시
- `ProjectHealthCard` — 사업계획서 완성도·품질 상태
- `AIRecommendationCard` — 코치 요다의 다음 행동 제안
- `GovernmentSupportCard` — 현재 단계에 맞는 지원사업 + "왜 지금" + 신청 CTA
- `DailyGoalCard` — 오늘의 진행 목표
- `LearningCard` — 현재 단계 관련 방법론 학습
- `NotificationCard` — 마감 임박 사업, 이어서 할 작업 알림

각 컴포넌트는 반드시 명확한 CTA를 내장해야 한다. 정보만 보여주고 끝나는 컴포넌트는 만들지 마라.

### 4단계 — Desktop / Mobile 별도 UX 설계

화면을 단순히 축소하지 말고, 각 플랫폼의 사용 맥락에 맞게 별도로 설계하라.

- Desktop: 넓은 화면을 활용해 현재 작업(좌)과 상태·추천(우)을 동시에 보는 작업 공간형
- Mobile: 이동 중 짧게 확인하는 맥락. "오늘의 미션" 하나에 집중하고, 스크롤로 상태를 확인하는 단일 컬럼형. 지원사업은 접이식으로 하단 배치

---

## 작업 순서와 산출물

1. 먼저 위 1~2단계(행동 정의 + 화면 설계안)를 **문서로 정리해서 보여주고, 확인을 받은 뒤** 코드에 착수하라. 바로 코드부터 짜지 마라.
2. 확인 후, 컴포넌트 구조(파일 트리)와 각 컴포넌트의 props·역할을 설계하라.
3. Dashboard(Home)부터 구현하라. 가장 중요한 화면이다.
4. 기존 기능(7단계 로드맵, AI 초안·피드백, 지원사업 추천, 완성도)은 그대로 유지하되, 배치와 진입 흐름만 행동 중심으로 재구성하라. 기존 백엔드 API 연동을 깨뜨리지 마라.

## 제약

- 기존 Next.js 구조와 라우팅을 최대한 유지하되, Roadmap을 Dashboard 하위로 편입하는 라우팅 변경은 허용한다.
- 기존 Solar API·완성도 채점·코치 요다 피드백 로직은 재사용하고, UI 레이어만 재설계한다.
- 각 단계마다 변경 사항을 설명하고, 큰 변경 전에는 나에게 확인을 받아라.
- 접근성(키보드 이동, 대비, 모바일 반응형)을 고려하라.

---

## 핵심 원칙 (판단 기준)

매 화면에서 스스로에게 물어라: **"이 화면에서 사용자가 해야 할 단 하나의 행동은 무엇인가?"** 그 행동을 가장 크고 명확하게 만들고, 나머지는 보조로 두어라. 정보가 주인공이 아니라 **행동이 주인공**이어야 한다.
